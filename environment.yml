name: metarl-iccbf
channels:
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - swig           # for box2d-py
  # nvcc matching torch's bundled CUDA (cu128 → 12.8.93).
  # This lets causal-conv1d and mamba-ssm build from source.
  - cuda-nvcc=12.8.93
